# Database Configuration
POSTGRES_DB=totp_sync
POSTGRES_USER=totp_user
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here

# Backend Configuration
NODE_ENV=production
PORT=3000
DATABASE_URL=postgresql://totp_user:CHANGE_ME_strong_password_here@postgres:5432/totp_sync

# Security Keys (Generate with commands below)
# JWT Secret: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32

# Encryption Key: openssl rand -hex 16
ENCRYPTION_KEY=CHANGE_ME_generate_with_openssl_rand_hex_16

# Features
REQUIRE_2FA_ON_REGISTER=true

# Frontend Configuration
VITE_API_URL=http://localhost:3000/api
